version: "3.9"
services:
  ai-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    # 리눅스에서 BACKEND_SERVER_URL=http://host.docker.internal:8080 해석을 위해 필요.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      pgvector:
        condition: service_healthy
    volumes:
      - model_cache:/app/model_cache
    deploy:
      resources:
        limits:
          memory: 2g
    networks:
      - ds_ai_network

  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: ai_server
      POSTGRES_USER: ai_server
      POSTGRES_PASSWORD: ${PG_PASSWORD:-changeit}
    ports:
      - "5433:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_server"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - ds_ai_network

volumes:
  pgvector_data:
  model_cache:

networks:
  ds_ai_network:
    driver: bridge
